import { createContext } from "react";

export const LanguageContext = createContext('en')


//the provider is a component that we can use to hold within it the value that 
    //we want to share to any component that is a part of its subtree.